use crate::*;
use crate::const_pool::ConstPool;
use crate::method_area::Class;

#[cfg(test)]
mod tests {
    #[test]
    fn test_adder() {
        let mut handle = std::fs::File::open("./Adder.class").unwrap();
    }
}